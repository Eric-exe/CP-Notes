When merging sets, merge the small set into large set. 
In scenarios like subtree sets, instead of $O(N^2)$ performance amortized, we get $O(Nlog^2N)$
[Small-To-Large Merging · USACO Guide](https://usaco.guide/plat/merging?lang=cpp)