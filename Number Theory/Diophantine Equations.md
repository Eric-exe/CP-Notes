Intuition: Integer solutions

Bezout's identity: $ax+by=\gcd(a, b)$

In other words, given:
$ax + by + cz + ... = e$, if $\gcd(a,b, c,...)$ divides $e$, then there is a integer solution. Integer solution can be found via Euclidean algorithm.